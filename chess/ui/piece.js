import React from 'react'
import { image } from 'react-konva'

const Piece = (props) => {
    /**
     * imageYUrl
     * color
     * id
     * color of player
     * if its your turn or not
     * if its player turn, color of player same as chessman
     * movable piece
     */
    return(
        <image>
            draggable
        </image>
    )



}
export default Piece